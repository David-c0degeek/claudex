# 03 — Attach And Phase Engine

## Goal
Bootstrap a run, register exactly one Claude and one Codex terminal to roles,
and drive the pairing state machine. `attach --agent claude|codex` binds a
session to lead or pair (no mid-run role switch; idempotent reattach; explicit
same-role session replacement so a crashed TUI never bricks the run). The phase
graph (PLAN_DRAFT → PLAN_CRITIQUE → PLAN_REVISE → IMPLEMENT_STEP → CHECKPOINT →
FIX → TESTS → VERIFY → DONE) advances on accepted `submit`s, each transition
durably issues the *next* assignment before releasing the lock, and convergence
is `AGREE` + zero blocking/major, evaluated by the coordinator.

## Integration analysis
> **Superseded by D013/D014 (Go greenfield):** the Python module names below are *reference-only requirements*, not reuse targets — implement fresh in Go (`internal/engine`, `internal/state`). The single authoritative phase-transition table/evaluator is new Go (03.4), informed by — not ported from — the old `phases.py:Orchestrator`. "Reuse" means the harvested test vectors from 00.2. Re-fill in Go terms before ticking any box.
> Fill/confirm against the 00.2 harvest before ticking any box.
- **New Go packages** — `internal/engine` (the authoritative submit-driven phase-transition table + evaluator + convergence check), building on `internal/state` (01) and `internal/transport` (02). Registration/session records live in `internal/state`.
- **Requirements harvested (D014)** — phase order, convergence definition (`AGREE` + zero blocking/major), lead-response budgets, and "planning stays planning" finding categories are drawn from the README + the old tests as *vectors*. The old Python `phases.py:Orchestrator` and `agents.py` adapters are **reference-only** for behaviour; no logic is ported — the Go transition table is written fresh against the vectors.
- **Behaviour to preserve** — phase order and convergence; lead-response budgets; fresh-verifier intent (D010, explicit).
- **Do not duplicate** — one phase-transition table/evaluator (`internal/engine`), one convergence check; no second engine anywhere (D003 — greenfield means there is no Python engine to coexist with, but the Go CLI must not grow a second one).
- **Integration point + why** — `internal/engine` wires accepted submits (02) to transitions and turn issuance (01.2); it enforces the filesystem classification (01.8) at bootstrap.
- **Closure dependency from 01.7 (legacy refusal seam)** — 01.7 shipped `legacy.CheckRunDir(dir)`, a read-only guard that fails closed on any existing `<dir>/state.json`: a detected pivot Python run returns `*legacy.LegacyRunError` (carrying `Remediation`); any other/unknown-schema state file returns `*legacy.UnknownStateError`. Attach state lives in immutable generation directories, never at `<dir>/state.json`, so an empty attach genstore alone is not evidence the directory is safe. The first-attach bootstrap (03.1) **must** call `CheckRunDir` before any allocation/resume mutation and fail closed on a non-nil result, so a legacy or unrecognized run directory is never allocated over. This is the wiring that lets 01.7 claim end-to-end resume/execution refusal; until it lands, 01.7's refusal is seam-only.
- **Vision fit** — realizes the README loop for real; lead-by-initiation (D001), no rotation; D010 verifier freshness.
- **Risks** — ensuring only the turn-owning role advances a phase; deriving faithful transitions from vectors without a Python crutch.

## Boxes
- [ ] **03.1** (agent) Run/session bootstrap as a two-step attach sequence (a single call cannot register two terminals): the **first** `attach` enforces the filesystem classification (01.8 — refuse/label `known-unsupported`, apply `unknown` policy) **and calls `legacy.CheckRunDir` before any mutation, failing closed on any existing `<dir>/state.json` — a pre-pivot Python run (`*LegacyRunError` + `Remediation`) or any unrecognized/unknown-schema file (`*UnknownStateError`) — since attach state never lives at `<dir>/state.json` (01.7 refusal seam)**, then atomically creates the run (snapshots task + base, allocates the worktree, chooses the lead), **reserves both agent/role slots**, registers the caller, and returns `run_id` + `session_id` + the exact pair-join command. The **second** `attach` fills the reserved pair slot. PLAN_DRAFT is issued only once both slots are registered. Uses the 01.4 repo-level allocation lock. Test: a `known-unsupported` state dir is refused at bootstrap; first attach reserves both slots + returns join command; second attach fills the pair; PLAN_DRAFT not issued until both present.
- [ ] **03.2** (agent) `attach --agent claude|codex --role lead|pair` binding + reattach + replacement: bind a terminal to a role in durable state; reject a mid-run role switch. **Reattach must present its existing `session_id`** — an idempotent reattach returns the same session; an attach that supplies no prior identity for an already-filled slot is a conflict and must use explicit **same-role replacement with an expected generation**, minting a new session generation so a crashed/abandoned TUI can be superseded (no leases, but replacement is possible). Auth is out of scope (same-user FS) — state so. Test: reattach with session_id idempotent; identity-less attach on a filled slot conflicts; replacement with expected generation supersedes.
- [ ] **03.3** (agent) In-transition assignment issuance: the state transition that accepts a submit durably issues the next role's immutable `turn_id`/assignment **before releasing the mutation lock**; the non-owning role's `pull` reads a `waiting` projection and never mutates. Test: after a submit, exactly one role has an actionable assignment and no identity is minted on pull.
- [ ] **03.4** (agent) Create the authoritative submit-driven **phase transition table + evaluator** in `internal/engine`, written fresh and driven by the harvested phase-order/convergence vectors (D014) — not ported from `phases.py:Orchestrator` (reference-only). Each phase's accepted artifact advances per this table; wrong-phase or wrong-role submit rejected. Test: full PLAN→…→DONE dry run over fake artifacts driven by the new table + the harvested transition vectors.
- [ ] **03.5** (agent) Convergence evaluation: `AGREE` + zero blocking/major closes a review phase; any blocking/major routes to the lead's fix phase. Reuse `schemas.py` finding categories. Test: mixed findings route correctly.
- [ ] **03.6** (agent) Lead-response budgets as phase counters: plan revisions, checkpoint fixes/step, test fixes, verify fixes — counters only increase; the threshold transition routes an exhausted budget to a human gate (subject 05 owns cap *policy/UX*; this subject owns the counter + transition). Exhaustion reports a quality-budget stop, not a false disagreement. Test: exhaustion produces the correct stop transition.
- [ ] **03.7** (agent) Phase edit policy: codify that the lead may mutate the repo **only** during IMPLEMENT/FIX; lead planning turns and **all** pair turns are repo-read-only. BYO attach checks this at `submit` (reject a repo mutation submitted from a read-only phase); managed launch also applies provider read-only flags (subject 07). Test: a mutation from a read-only phase is rejected.
- [ ] **03.8** (agent) VERIFY blocks for a fresh session generation (D010, single locked behavior — no downgrade branch): the incumbent pair generation is invalid for VERIFY; the phase **blocks until a new same-role session generation attaches**. BYO reports `fresh-session-declared` (the session *generation* is enforced; model-context freshness is not). Managed reports `fresh-process` only when it actually launches a new process. Changing this to allow a downgrade requires a later explicit decision/gate. Test: VERIFY does not proceed on the incumbent generation; a new generation unblocks it; labels match the tier.
- [ ] **03.9** (agent) Invariant: **`attach` is the sole run bootstrap** (03.1) — there is no headless `run` command and no old `pair` driver (greenfield; the Python engine was deleted in 00.3). Assert the exact command surface is the attach protocol (`attach`/`pull`/`submit`/`wait`/`status`/gates/operator) plus the read-only legacy **inspect/export** utility (01.7), and nothing else. Test: the only run-creating command is `attach`; there is no `run`/old-`pair` entrypoint; the legacy inspector is read-only and refuses execution/resume.

## Hindsight checkpoint
- [ ] Captain Hindsight review recorded
- [ ] Verdict is `CLOSE`

## Progress log
> One line per slice.

- 2026-07-17 · slice o · checkpoint-3 cleanup (CX TURN 120 REVISE → 5 fixes) · **(1)** two legal replaceable shapes, not Terminal-only: a pre-pair **lead** slot (`JournalAbsent` + pristine joinable INIT, reusing `requireJoinableInitShape` with `authorizeReplace` now returning the guarded CurrentRun; the empty pair slot → `ErrReplaceSlotEmpty`, JoinAttach still succeeds after), and a **Terminal** journal whose durable EFFECTS landed (`requireCompletedPairEffects` reuses `decodePairIntent` + `planDraftApplied`: the recorded pair session must be in pair history with the bound role/agent AND `FirstTurn` proves issuance) — a terminal-with-rolled-back/missing effects, a vanished completed journal (Absent + filled pair), a pending/non-terminal, or a corrupt/mis-bound journal are all recovery-required; a pending pair txn still blocks lead replacement (frozen lead digest). **(2)** exact-release-once on every path: reverse-order `errors.Join`'d releases on all guarded rejection returns (no error-discarding deferred releases), via per-call injected `replaceSeams` (release + mutate). **(3)** `MutateLocked` `genstore.ErrAmbiguous` → a distinct `ErrReplaceOutcomeUnknown` path that RETAINS the candidate identity for recovery (never in `CommitWarning`, which stays reserved for a proven commit) — a proven-uncommitted failure still returns a zero result. **(4)** evidence: pending-journal + terminal-missing-effects (deleted state/registry) recovery rejects, optimistic collision retry + exhaustion (using a real existing session id's bytes), the coordinator `TestE2EReplacedSessionUnauthorized` now drives the REAL `attach.ReplaceAttach` (old-session→`ErrUnauthorized`/new-session→submit-authorized), concurrency classifies the loser (busy/stale) + retry-is-stale + the untouched slot stays byte-identical, and seam tests pin the joined-release/abort/post-commit-warning/ambiguous-candidate/proven-uncommitted paths. **(5)** paths via `runLocationFor` (`RegistryDir`/`RunLock`/`StateDir`/`AttachDir`); taken via the Registry's `Resolve(id).Status != RegUnknown` (removed the duplicate scan). Full build/vet/gofmt/`test -race ./...` + 4-target cross-compile green. Sent as checkpoint-3 cleanup (TURN 121).
- 2026-07-17 · slice n · checkpoint 3: explicit same-role replacement API (03.2 replacement half) · CX AGREE'd checkpoint 2 (TURN 119, signed off through `7eaff87`+`30816c1`+`b6a1cde`). New `internal/attach` leaf `ReplaceAttach(ReplaceRequest{RepoDir, RunID, Role, Agent, ExpectedGeneration, RNG}) (ReplaceResult, error)` closing the unimplemented half of 03.2: it supersedes the current session of a filled role slot with a new session generation so a crashed/abandoned TUI is taken over WITHOUT disturbing RunState (the tension `wait`'s separate-session seam + the separate Registry store were built for). Flow: pre-mint the new session id OFF the guards from an optimistic complete taken-session set → acquire the repo guard → `authorizeReplace` (reuse `bindRunToBootstrap`: active-pointer→catalog→bootstrap-journal authority, no lock-free TOCTOU) → acquire the run guard → require `ClassifyPairJournal(g, loc)==Terminal` under the live guard (pending/aborted/vanished/corrupt/mis-bound → typed `ErrReplaceRecoveryRequired`) → load the LOCKED Registry and require run-id match, slot filled, agent match (mid-run switch → `ErrReplaceAgentMismatch`), `current_generation==expected` (stale → `ErrReplaceStaleGeneration`, never leaking the current session credential) → recheck the pre-minted id canonical + globally unique under the lock → drop the repo guard → ONE `Registry.MutateLocked` appending exactly one session (other slot byte-identical, generation=previous+1, `IssuedRegistryRevision`=appended revision, `CurrentSessionID`=new) as the SOLE durable effect (no RunState revision/assignment change). Post-commit honesty: a committed append with a failed run-guard release returns the result with a `*genstore.PostCommitError` `CommitWarning` (never an uncommitted-failure report, never discards the new identity); both guards released exactly once with joined errors. Tests: success (pair + lead), wrong agent, stale generation (+ no-credential-leak + replay-after-replace), unauthorized non-active run, not-yet-paired + vanished-journal → recovery, RNG failure, request validation matrix, concurrent replacements (exactly one winner, exactly one appended session), and no-RunState-advance; old-session→`RegReplaced`/new-session→`RegCurrent` proves the submit-authorization ordering at the Registry (the authorizer's sole source). Reuses existing attach/state readers + `ClassifyPairJournal`; no parallel allocation/journal logic; no CLI/TESTS/VERIFY/gate-resolution/coordinator changes. Full build/vet/gofmt/`test -race ./...` + 4-target cross-compile green. Sent as checkpoint-3 review (TURN 120).
- 2026-07-17 · slice m · checkpoint-2 evidence/seam cleanup (CX TURN 118 REVISE → 4 test/seam fixes) · **(1)** deterministic precompute→transport stale race: `TestE2EStalePrecomputeRace` uses an `afterPrecompute` barrier to advance the durable state under the real run guard (reissue the assignment at the next generation) AFTER facts/ids are captured, then asserts `*transport.StaleError`, the revision advanced by exactly the deliberate reissue, the blocked turn unaccepted, no artifact published, and — via a counting RNG — the pre-minted pair consumed exactly 32 bytes and NOT regenerated after release. **(2)** deterministic concurrent fact-prep: a `sync.WaitGroup(2)` + `proceed` barrier on `afterFacts` proves BOTH submits complete `loadFacts` off the guard before either enters transport, retaining the exactly-one-fresh/promote-once assertions (clean under `-race`). **(3)** replaced the mutable `Run.beforeTransport` field with per-call context-carried `submitHooks{afterFacts, afterPrecompute}` (nil in production) — test synchronization is out of `Run` state, and the stale/fact-prep/close tests compose without a shared callback; Close-waits now places its barrier during precompute (`afterFacts`), covering the in-flight precompute lifetime. **(4)** focused loader boundary tests: over-count rejects with `engine.ErrHistoryTooLarge` before any `Get` (empty store proves ordering), a missing referenced artifact is `ErrEvidence`, and ~135 padded ~64 KB plan artifacts overflow the 8 MiB cumulative bound → `ErrHistoryTooLarge`. Removed the non-deterministic DRAFT concurrency test (subsumed). Full build/vet/gofmt/`test -race ./...` + 4-target cross-compile green. Sent as checkpoint-2 evidence cleanup (TURN 119).
- 2026-07-17 · slice l · checkpoint-2 cleanup (CX TURN 117 REVISE → 4 fixes + evidence) · **(1)** Prepare no longer does I/O or RNG under the run guard: `Run.Submit` now PRECOMPUTES off the guard — `Normalize` + optimistic `State.Load`; an already-accepted turn gets a fail-Prepare (no facts/RNG, same+different-digest replay both RNG-independent); a new turn loads/materializes facts read-only against the optimistic state, compares all refs there, then under `mintMu` pre-mints BOTH assignment+gate candidates once (turn added to `taken` before the gate mint); the closure handed to transport does only `compareRefs(locked)`→`Project`→`Evaluate`→`RequiredID`→pick the captured id→`NewPreparedTransition`, Apply=`engine.Apply`. Busy-acquire retries reuse the captured candidates. **(2)** `loadFacts` bounds before loading: collect+sort durable planning refs, reject count>`MaxPlanningArtifacts`, then load in receipt order enforcing the cumulative byte cap before each append; missing/corrupt evidence → typed `ErrEvidence` before the lock. **(3)** journal fail-open closed: `pairJournalReader.Head` now maps NonTerminal→recovery and **Absent/unknown/error→Unknown+error** (a Run opened only after pairing must never see the completed journal vanish and proceed). **(4)** `attach.RunLocation` gains `ArtifactsDir` (coordinator no longer path-joins); `OpenRun` preserves classification+release errors via `errors.Join`; removed the `filepath` import. Evidence extended: the real chain now includes a plan reviewer retry, a checkpoint reviewer retry, CHECKPOINT→FIX, FIX→CHECKPOINT; separate real runs end at plan-quality (PlanRounds=0) and checkpoint-quality (CheckpointRounds=0) gates; a vanished-journal run → recovery-required + no artifact; concurrent same-turn submits at PLAN_CRITIQUE prepare facts concurrently and linearize to one fresh accept; Close blocks on an in-flight submit (barrier); a direct Registry replacement supersedes the pair session → `ErrUnauthorized`, the new session promotes. Replay-RNG updated to 32 bytes (two candidates) on accept / zero on replay. Full build/vet/gofmt/`test -race ./...` + 4-target cross-compile green. Sent as checkpoint-2 cleanup (TURN 118).
- 2026-07-17 · slice k · checkpoint 2: production coordinator wiring (03.3/03.4/03.5/03.6 wired) · CX AGREE'd checkpoint 1 (TURN 116, closed at `2420928`+`5b59e4e`+`1858de9`+`d73b577`). New `internal/coordinator` binds the pure engine to the durable stores behind one entrypoint: `OpenRun(repoDir, runID, rng)` resolves the run via `attach.ResolveRun`, fails fast unless `ClassifyPairJournal` (under a held run guard) is Terminal, and opens the real `ArtifactStore`; `Run.Submit` drives `transport.Submit` with an engine-backed `Prepare`; `Run.Close` waits out in-flight submits (an `RWMutex` RLocked for each submit's lifetime, Locked by Close) so it never races an artifact Get/Put. The `Prepare` seam: reconstructs facts ONLY for PLAN_CRITIQUE/PLAN_REVISE by filtering the accepted planning turns, loading each exact `(turn,digest)` from the ArtifactStore, folding via `engine.MaterializeCandidate`, then cross-checking EVERY ref (`compareRefs`: plan source/digest/count, check keys+digest, ExpectedPhase, PendingFindings source/keys/nil) against the locked snapshot before `Project`→`Evaluate`→`RequiredID`→one mint (RNG serialized by a `mintMu`, complete optimistic taken set)→honest `PreparedTransition` whose apply calls `engine.Apply` with exactly that decision+ids; it fails closed on a replay-in-prepare, an unsupported edge, or fact drift, before any Put. `pairJournalReader.Head(g,runID)` requires `runID==loc.RunID`, exhaustively maps the three attach classes, and treats an unknown class/error as recovery-required; `LockPath==loc.RunLock`. Real-store e2e (real FirstAttach+JoinAttach+ArtifactStore/journal/Registry): the full non-gated chain plan→critique(revise)→revision→critique(agree/promote)→implement→checkpoint(next)→implement→deferred-TESTS edge; a separate human-gate run pauses; stale-revision submit → StaleError; replay skips RNG (16-byte reader exhausted after the one real mint, replay still re-confirms); two concurrent submits linearize to exactly one fresh acceptance; OpenRun refuses an unpaired run; Close refuses further submits. `compareRefs` drift guard unit-tested per field (state validation prevents a genuinely-drifted state persisting). No TESTS/VERIFY/gate-resolution/replacement/CLI. Full build/vet/gofmt/`test -race ./...` + 4-target cross-compile green. Sent as checkpoint-2 review (TURN 117).
- 2026-07-17 · slice j · checkpoint-1 cleanup round 2 (CX TURN 115 REVISE → 3 fixes + 2 hygiene) · **(1)** `CandidateRefs.CheckKeys` copies with `append([]string{}, …)` so the canonical-empty check set (normal state right after the first committed draft) stays a NON-NIL empty array; added a draft-only test (non-nil zero-length keys, empty digest, `ExpectedPhase=PLAN_CRITIQUE`, successful `verifyCandidateFacts` round-trip for the first PLAN_CRITIQUE). **(2)** the fold now binds submitted revisions across consecutive receipts — `verifyArtifactIdentity` requires `prevReceipt <= state_revision < thisReceipt` (a turn accepted after receipt R was submitted at/after R; gaps only raise it) — and rejects a duplicate turn id via a `seen` set; fixtures made realistic (`state_revision >= prior receipt`), with new stale-history and duplicate-turn negatives. **(3)** the shared `routeTable` RouteGate edge now requires `isSubmitPhase(from) && next==AWAIT_GUIDANCE` (was `next`-only, which accepted a non-submit origin like DONE→AWAIT), so `RequiredID` rejects an illegal gate origin before minting; added a non-submit-origin negative. **Hygiene:** dropped the hand-mutated "aborted" classifier record (the genuine pending record already covers the NonTerminal branch) and added a `defer` reset of `stepFailpoint`. Full build/vet/gofmt/`test -race ./...` + 4-target cross-compile green. Sent as TURN 116; still no coordinator code.
- 2026-07-17 · slice i · checkpoint-1 cleanup (CX TURN 114 REVISE → 6 fixes) · **(1)** unified route metadata: one `routeTable{route→(IDKind, edge)}` in `engine`; `routeAllowed` and `RequiredID` both consume it (no second switch), and `RequiredID` now rejects an unknown route, an illegal from→next edge, AND a gate-presence mismatch before any mint (the old version blessed a zero/illegal decision). **(2)** the history fold now `protocol.Validate`s every artifact against its phase's schema and requires the stored bytes to equal the canonical form, so a digest-correct artifact with a missing/extra/invalid field (which transport could never accept) is rejected. **(3)** removed the duplicate parsers: one shared `materializePlan` (live draft + fold) and one `projectCritiqueParts` returning the full materialized result/actionable-keys/verdict/missing/target-validity (live critique + fold), deleting `materializePlanArtifact`/`materializeCritiqueArtifact` and the silent second `actionableKeys` reparse; `applyRevision` stays shared. **(4)** `CandidateRefs` now also carries `ExpectedPhase` and the reconstructed `PendingFindings` (source+keys, deep-copied, nil when none), so a well-shaped state carrying a different pending set/expected phase than the immutable history can't pass the coordinator's cross-check. **(5)** `ClassifyPairJournal(g, loc)` opens the journal and proves the live run guard via `CheckGuard` before reading (no lock-free race); `classifyPairRecord` binds the intent (version/kind/txn-id/expected-state-revision/run-id) BEFORE the terminality branch, so a pending/aborted record is bound-then-classified and a mis-bound one errors. **(6)** overflow-safe byte accumulation (`len > max-total`); added focused negatives — RequiredID illegal-edge/gate-mismatch/unknown + Evaluate-produced parity; schema missing-required/unknown-property/invalid-verdict; pending+ExpectedPhase binding; deep-copy/alias-mutation across calls; guard nil/released/wrong-lock; and a **genuine** pending pair record (crash after `registry-pair-fill`) instead of an impossible hand-mutated one. Full build/vet/gofmt/`test -race ./...` + 4-target cross-compile green. Sent as checkpoint-1 cleanup (TURN 115); still no coordinator code.
- 2026-07-17 · slice h · production-wiring checkpoint 1: leaf/shared surfaces through engine materialization (03.4 wiring prep) · The reusable, side-effect-free surfaces the coordinator (checkpoint 2) will compose, each with focused + full tests, no coordinator code yet (blast-radius control agreed with CX). `internal/engine` — `MaterializeCandidate(arts []AcceptedArtifact) (ProjectionFacts, CandidateRefs, error)`: the ENGINE-OWNED replay that reconstructs the current pre-agreement candidate plan/check facts from the immutable accepted PLAN_DRAFT/CRITIQUE/REVISE history by re-deriving (via the same routing the live engine uses) WHICH accepted turns committed — human-gated and non-actionable reviewer-retry turns are accepted-but-commit-nothing, so they are validated then discarded (durable AcceptedTurns refs alone can't reconstruct facts; the fold must re-run the routing). Verifies per-artifact identity (strictly-increasing receipt revision, digest==sha256(canonical), envelope turn_id/message_type↔phase, state_revision∈(0,receipt)), tracks the expected phase chain, rejects any history the live engine would (converged critique can't be a pre-agreement candidate; bad base/responses; out-of-order), and bounds inputs (MaxPlanningArtifacts/MaxMaterializeBytes). `RequiredID(dec) (IDKind, error)` derives the issued identity (gate vs assignment) from the SAME route table Apply consumes, failing closed on an unknown route. `ProjectionFacts` gains `CandidateSource` + a Project rebind so a swapped history that reproduces the plan bytes can't pass as the current candidate. Shared `applyRevision` extracted so the live projector and the history materializer share one revision-materialization. Leaf helpers finalized with focused tests: `state.MintTurnID/MintGateID` (shared minter, taken-skip, exhaustion, nil-RNG), `txn.(*Journal).CheckGuard` (live/nil/released/wrong-lock), `transport.Normalize` (order-independent canonicalization, redact-before-digest, envelope + decision_question presence). `internal/attach` run-resolution leaves (reuse, not new authority): `ResolveRun(repoDir, runID) (RunLocation, error)` binds the run through the existing active-pointer→catalog→bootstrap-journal authority (`bindRunToBootstrap`) after the legacy-refusal guard and returns only canonical attach-derived paths (run dir, run lock, state/registry/attach dirs) so the coordinator does no path joins of its own; `ClassifyPairJournal(loc) JournalClass{Absent|NonTerminal|Terminal}` strictly binds the pair-attach journal head (version/kind, txn id, expected state revision, run id) before calling a pairing complete. Focused tests: resolve binds/derives every path, rejects a non-active/non-canonical/no-active run; classify is Absent pre-pair, Terminal post-pair, and NonTerminal/identity-error on every corrupted-record branch. Full `go build/vet/gofmt/test ./...` + `-race` + 4-target cross-compile (linux/amd64, windows/amd64, darwin/arm64, linux/arm64) all green. **Disclosed library-only surfaces (wire-or-delete): `state.MintGateID` and `txn.(*Journal).CheckGuard` have no non-test caller yet — they are the gate-id minter and guard-liveness check the checkpoint-2 coordinator (Prepare adapter / gate issuance) will wire; kept now as the coherent sibling of the already-wired `MintTurnID` / internal `CheckGuard` uses.** Sent to CX as checkpoint-1 review; awaiting AGREE before starting `internal/coordinator` + the real-store e2e/race layer (checkpoint 2).
- 2026-07-17 · slice g · locked Submit reauthorization + pre-sink ordering (03.3 barrier) · `internal/transport` Submit rewritten to hold ONE run guard across authorize→publish→accept, closing the pre-lock authorization race (auth + sink.Put previously ran lock-free before the CAS). New `SubmitDeps{Store, Registry, Journal, Sink, Prepare, Preflight?}` with lock-path equality asserted; ordering under the guard: load RunState → classify the attach journal FIRST (`JournalReader.Head`, zero=unknown, exhaustive, terminal/absent proceed else typed recovery-required, no sink — before the registry so a crash routes to recovery not run-mismatch) → registry load + `reg.RunID==rs.RunID` → definitive `Resolve(sessionID).Status==RegCurrent` → same-turn replay with **role-before-digest** (`requireCurrentRole(TurnSpec(acc.Phase))` so a wrong-slot session gets Unauthorized not Conflict and never re-confirms the sink) → new-acceptance stale/live/turn/role → schema (raw+redacted) → `Prepare(deepCloneSnapshot, prepared)` returning a typed `PreparedTransition{issuedTurnID, issuedGateID, apply}` → recheck issued ids (canonical, one-of, shared turn/gate namespace, not consumed/accepted) → recheck ctx → `sink.Put` → `MutateLocked(g)` (apply, nil-map recheck AFTER apply, requireLiveOwner, `bindIssued` declared==applied, accept). Release exactly once per path (`releaseOutcome` joins pre-acceptance errors, wraps post-commit as PostCommitError); a MutateLocked failure returns (deterministic; the crash-orphan is re-confirmed by a fresh submit). The engine adapter is NOT wired to production (Prepare is a seam). CX AGREE (`86006ed`→`5c61cb6`, TURN 105→110, ~6 rounds — caught the acceptance-before-publish inversion, replay role bypass (twice: role, then role-before-digest), declared-id-not-bound-to-apply, journal fail-open, discarded release errors, missing ctx recheck before Put, the probe adapter's early-Apply, and journal-before-registry recovery order). **03.3 locked**; next is the production wiring of `engine`→`Prepare` on this path, then TESTS/ownerless-VERIFY.
- 2026-07-17 · slice f · phase engine (03.4/03.5/03.6 pure core) · `internal/engine` — the sole legal phase-edge/convergence table as a PURE package (no I/O, no id minting, imports only state/protocol/canonjson): `Project(cur, canonical, ProjectionFacts)` derives a typed Event from a schema-valid artifact + loader-supplied materialized plan/check facts (rechecking their digests/keys against the durable candidate, semantic-error on any state inconsistency — wrong revision base, non-exact revision responses, remove-missing check, dangling obligation at convergence, empty materialized prose); `Evaluate(cur, ev)` routes with a human-decision-first predicate over the harvested convergence rule (AGREE ∧ ¬missing ∧ ¬actionable, checkpoint also ∧ tests_adequate), three-way reviewer-retry, and plan/checkpoint quality budgets (counters advance only on the running fix/revise edge; used>limit fails closed; wrong-kind-for-phase rejected); `Apply(dec, submitted, ids, gen, next)` realizes a decision into an already-cloned next, with a TOTAL pre-mutation validator (source binding to phase/revision/event, live pre-state, route↔edge table, exact gate subset table bound to the from-phase, running/gate budget rechecks so a forged over/at-limit edge can't bypass the sole budget table, id canonicality + non-reuse, and full payload value-validation incl. the empty-check biconditional) so a malformed decision never panics or partially mutates, binding every issued identity to the gap-skippable resulting generation and copying payloads into state. The final-checkpoint→TESTS and FIX→VERIFY edges are deliberately `ErrPhaseUnsupported`; an unexposed test adapter drives Project→Evaluate→Apply through the real schema (protocol.Validate + CAS-revision assert) simulating transport's post-apply accept, and is NOT wired to production Submit. Added `protocol.MaxKeySetItems` + a schema `maxItems` parity, and `plan_revision.plan_markdown` `minLength:1`. CX AGREE (`0c21c48`→`c406039`, TURN 100→104, 5 rounds — caught the impure-Project signature, three-way reviewer-retry regression, revision-as-precondition-not-gate, materialized-vs-submit digest, dropped check content in the digest, the plan-gate producing an invalid v5 state, wrong-kind not rejected, Apply panics/partial-writes/budget-bypass on forged decisions, the gate-origin binding gap, and the empty-check biconditional). **03.4/03.5/03.6 core landed; NOT production-wired** — the barrier is the under-run-lock Submit reauthorization + pre-sink ordering (next slice), then TESTS/ownerless-VERIFY on the same locked path.
- 2026-07-17 · slice e · state v5: phase-engine working set (03.4/03.5/03.6 durable substrate) · `internal/state` RunState schema v4→v5 — the durable fields the fresh phase engine reads/advances, with state owning ONLY local shape/provenance (no phase edges): plan-negotiation staging (CandidatePlan by materialized-doc digest + accepted-submit EventRef source; the materialized CheckSetRef obligation set; PendingFindings by raising critique), the write-once immutable PlanAgreement, the inclusive StepIndex cursor + exact per-step StepFixes vector, the ownerless-first VerifyRequirement threshold (RequiredGeneration; attempt derived VerifyFixes+1), and one durable PauseContext unifying human-decision and quality-budget gates. Enforces: four-way gate coherence (pause==gate==AWAIT==paused; gated ⇒ no assignment); effective-suspended-phase phase-family ownership; clear-together promotion in one exact generation with a fresh-zero StepFixes vector; cursor bounds/position; verify/fix presence with transfer-into-pause and restore preserving the value (never a consumed turn); accepted-event provenance (no EventKind — phase is the authoritative acceptance fact) with receipt cross-bindings (critique receipt==AgreedRevision, agent gate source receipt==Gate.IssuedRevision); strictly-sorted count-bounded canonical key sets with a pinned canonical empty-set digest (biconditional); a shared storage-safe counter ceiling (config.MaxBudget=1<<14, so a frozen max_run_turns always fits the 16 MiB record cap); honest quality-budget labels (the named counter already at its frozen limit, proven against the pre-denial counter by freezing counters+cursor across the gate opening); and paused immutability (pause/gate/cursor/counters frozen while paused). Also step-1 prerequisites: config task-criteria exact-uniqueness (value-free), protocol MinPlanSteps/MaxPlanSteps + MaxKeyLength + MaxKeySetItems with schema parity, IsKey/ValidateKeySet/ValidateStepTitles. Transport/attach fixtures now drive the real negotiation to reach an implementation phase; `normalizeToBaseline` strips the v5 set. CX AGREE (`6c2692d`→`9292a9b`, TURN 91→98, 6 rounds — caught issuance-vs-acceptance provenance conflation + materialized-vs-submit digest split, ownerless VERIFY (RequiredGeneration not a turn), the two-kind Pause union + BudgetKind belong in v5, empty-slice clone-to-null, unbounded CandidateChecks lifecycle + key-set counts, mutable-while-paused, missing enum redaction, and the gate-opening counter/cursor charge that defeated budget honesty). **03.4 stays OPEN**: this is the durable substrate; the `internal/engine` Evaluate/Apply + projector + transition table (the production writer) is the next slice.
- 2026-07-17 · slice d · reattach (03.2 read-only path) · `internal/attach` Reattach — resolves an existing session against a run, strictly READ-ONLY + LOCK-FREE (no append/recovery/mint/dir/lock-file creation; lead reattach precedes the run lock's existence). Bracketed stable-snapshot protocol: read CurrentRun + attach-journal head, snapshot Registry, re-read both, require BOTH unchanged around the Registry read (a run switch → stale; either bracket nonterminal → recovery-required, checked before the changed-head test; a changed-but-terminal/missing head incl. missing→complete → stale). Agent/role matched first for any known resolve, then classify current/replaced/unknown; replaced returns the current generation for remediation but never a credential; injectable `reattachReader` seam (no global hook) drives deterministic race tests; byte-for-byte FS non-mutation pinned. CX AGREE (`499f6bc`→`56cc4f3`, TURN 86→87, 2 rounds — caught the unbracketed journal race + role-first-for-replaced).
- 2026-07-17 · slice c · pair attach (03.1 second half + 03.2 join) · `internal/attach` JoinAttach — fills the pair slot and advances INIT→PLAN_DRAFT issuing the lead's first turn, one run-guarded journaled transaction. Closes the repo→run race (authorize the exact bootstrap allocation under the repo guard — active pointer + catalog ref == `bi.runRef()` + complete fully-bound bootstrap journal whose identity equals the pointer — take the run guard in fixed repo→run order, and revalidate the complete pristine-INIT shape (revision 1, zero budgets) before releasing repo). Split minimal-retry vs new-pair validation; frozen intent carries lead/INIT-baseline digests + clock; `pairPlanFor` classifies state as exact baseline or applied descendant + validates clock before any effect; observations are exact frozen baselines. The durable issuance proof is the new write-once `RunState.FirstTurn` (schema v4) — issued only on the exact INIT→PLAN_DRAFT transition equal to the assignment, immutable thereafter — so recovery recognizes the exact first turn through Submit/cancel/operator alike and a forged id can't be claimed. Recorded barrier: future lead/pair **replacement** must recover a pending attach journal first under the run guard (LeadDigest not monotonic through it). CX AGREE (`bda0061`→`b9aaf84`, TURN 81→85, 5 rounds — caught the authorization race, the non-frozen pre-state, the timestamps-aren't-turn-identity gap → FirstTurn, and the pristine-INIT/clock invariants).
- 2026-07-16 · slice b · first-attach bootstrap scaffold (03.1 first half) · `internal/attach` — a repo-scoped, journaled first attach. A deterministic `BootstrapIntent` is prepared READ-ONLY before the journal (mints run/session/txn/operation ids, runs legacy refusal + fs classification before any mutation, resolves the base commit from a frozen OID, embeds the parsed+validated task/effective-policy snapshots, and fixes the exact derived layout); idempotent Observe/Apply participants (snapshot-task/policy rooted no-clobber at the repo, worktree seam, registry-init, state-init, catalog-allocate, and a journaled current-clear+current-run-set) realize it under the repo allocation lock, so a crash between an applied effect and recorded progress is repaired forward and recovery re-derives the identical plan. Adds RunState schema v3 (frozen workspace identity), a separate `CurrentRun` active-run pointer with typed Activate/Clear (terminal-bound reconcile, absorbing-lifecycle-unresurrectable so history isn't single-run), and `state.IsOperationID`/`MintOperationID`. Lost-response idempotency is a combined identity-bound read across the COMPLETE bootstrap journal + active-run pointer + Registry (registry is the sole session authority; a replaced session never returns). fs refusal is mutation-free (lock-free probe → read-only classify → refuse before `.claudex`). Git base-resolution + worktree creation are seams subject 04 fills. CX AGREE (`0620afb`→`874bf69`, TURN 75→80, 6 rounds — caught config-contract bypass, missing workspace persistence, dead legacy refusal, mutation-before-fs-refusal, forged-intent layout/authority, replace-capable/path-based snapshots, weak Observe, non-idempotent completed-response, singleton-catalog, split-brain clear, duplicated session authority, frozen-prior enforcement, and complete-not-aborted return). **03.1 stays OPEN**: needs the real git worktree provisioner (subject 04) + pair attach (next slice).
- 2026-07-16 · slice a · registration store foundation (for 03.1/03.2) · `internal/state` Registry — a SEPARATE immutable-generation store under the SAME per-run lock as RunState, so a same-role session replacement supersedes a crashed TUI WITHOUT advancing `RunState.Revision` (which would invalidate the other role's live, revision-bound assignment — the tension `wait`'s separate session seam was built for in 02.4). `Registry{RunID, Revision, Lead/Pair *RoleSlot}`, `RoleSlot{Agent, CurrentSessionID, Sessions []SessionRecord}` (append-only, generations 1..N, agent immutable once filled), and a single value-only `Resolve(sessionID) -> {Role, Agent, Status∈{current,replaced,unknown}, CurrentGeneration, CurrentSessionID}` shared by the submit Authorizer/wait/status. Invariants: exactly one attach/replacement per revision (init = lead + one gen-1 session, pair null; later = exactly one new session globally, other slot byte-identical; no no-op generation); distinct agents; globally-unique **exact-minted** session ids (`sess-`+32 lower-hex, exported `state.IsSessionID`, enforced at the assignment wire + both inbox path boundaries so ids-as-dir-names can't case-alias); callback can't poison generation identity (revision/schema guarded before commit, fixed in both stores); `MintSessionID` injectable-RNG + secret guard + collision-retry/fail-closed. `MutateLocked` composes with RunState under one guard (serialized, not crash-atomic — the journal owns cross-store atomicity). CX AGREE (`b4b39b6`→`8b3c0dc`, TURN 70→73, 4 rounds — caught RunState-embedded-registration (→ separate store), impossible init/bulk/no-op shapes, callback revision poisoning, loose session grammar at the path/wire boundaries, and the missing exhaustion/same-agent/read-path cases).
