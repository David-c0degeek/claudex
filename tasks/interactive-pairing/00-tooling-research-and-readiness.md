# 00 — Evidence Harvest, Go Toolchain And Readiness

## Goal
Prepare a greenfield **Go** rebuild (D013). Harvest the language-neutral assets
from the old Python code as executable requirements (D014) — schemas, fixtures,
golden outputs, and the 17 refactor-branch test files / crash-cut scenarios —
without porting a line of the old implementation. Stand up the Go module,
package skeleton, and toolchain; research the OS primitives, canonical-JSON, and
provider-launch facts the later subjects depend on; and write the protocol ADR.

## Boxes
> Subject 00 is required. No implementation subject starts until this subject is
> `DONE`, `ABANDONED`, or waived by decision. A non-green Go skeleton baseline
> from 00.3 blocks implementation subjects unless a §4 row accepts it.

- [x] **00.1** (agent) Read repo instructions + authoritative docs. `README.md` is present on `main`; `docs/architecture.md`, `docs/decisions.md`, `tasks/lessons.md`, `tasks/claudex-refactor/`, `CHANGELOG.md` exist **only in refactor-branch history** — read them via `git show reliability-observability-refactor:<path>`. List the intended pair-programming direction + constraints so later subjects flag drift. Record the vision→code gap (README describes the loop; the Python code drove subprocesses).
- [x] **00.2** (agent) **Evidence harvest (D014), no code porting.** From `reliability-observability-refactor` git history (`git show <rev>:<path>`), catalog the reusable *assets*: the **10 `test_*.py` modules + 6 fixture executables**, golden outputs, JSON schemas, and crash-cut/adversarial scenarios. Tag each behaviour with a **disposition** (`ADOPT | ADAPT | CONDITIONAL(07) | RETIRE`) so 01–06 do not rebuild the retired headless engine. Turn the subtle-behaviour cases into a Go test-vector inventory — especially git **tree identity** (tracked/untracked/ignored, symlinks, submodules, clean/CRLF filters, case-only paths, staged dirt, racing edits), **process cancellation**, **redaction boundaries**, and **usage normalization**. Output: a mapping from each harvested vector → the Go subject that must satisfy it. Explicitly record that the Python modules are reference-only.
- [x] **00.3** (agent) Stand up the Go module + skeleton and a green baseline: `go.mod` (module path + Go version), and a **capability-split** package layout — `cmd/claudex`; `internal/state`, `internal/txn`, `internal/fsclass`; `internal/transport`, `internal/protocol` (embedded schema bytes), `internal/canonjson`; `internal/engine`; `internal/gitx`, `internal/evidence`; `internal/gates`, `internal/caps`, `internal/lifecycle`; and per-capability OS packages `internal/oslock`, `internal/atomicfile`, `internal/proctree` (each build-tagged `_windows.go`/`_unix.go`), with `internal/pty` deferred to 07.3 only if needed; `internal/launch`, `internal/provider` for subject 07. The full package map above is **documented** as the target architecture, but 00.3 **creates only the packages the current slice needs** (e.g. `cmd/claudex` + a minimal `internal/*`) — no placeholder `doc.go`-only packages just to match the map; later subjects add their packages when they implement them. A hello-world CLI so `go build ./...`, `go vet ./...`, `gofmt -l .`, `go test ./...` all pass. Remove the Python implementation from the working tree on this branch (kept in git history per D014) and immediately rewrite `README.md` to an honest Go/build-in-progress surface — no broken Python install commands left behind. Confirm/correct the §2 Verification-commands table.
- [x] **00.4** (agent) Research + choose the OS-primitive mechanisms (D015) for Windows + POSIX, each destined for a build-tagged package: advisory locks with **process-death semantics** (`internal/oslock`), a **crash-stale reclaim** signal (OS-held lock, or nonce + process-start-time identity), atomic rename + directory durability + sharing-violation retry (`internal/atomicfile`), and process-tree kill (`internal/proctree`: Windows create-suspended→assign-job→resume; POSIX process groups). Research the **filesystem classifier** approach (`internal/fsclass`, D015): how to detect SMB/NFS on Windows + Linux and the honest limits on detecting arbitrary sync roots. Pick the canonical-JSON approach (RFC 8785 or restricted equivalent). Record which mechanism 01.4 / 01.8 / 04 implement.
- [x] ~~**00.5** provider interactive/telemetry research~~ **RELOCATED to subject 07** (managed launch is the only consumer; irrelevant to 01–06). Recorded as decision-log note; subject 07 owns "how Claude Code + Codex behave as interactive sessions, inherited-stdio launch + flags, and whether a usage/cost telemetry side-channel exists (D007 — no metered caps otherwise); ConPTY/PTY only if telemetry needs it." This closes 00.5 in subject 00 (a scoping decision), not by doing the research here.
- [x] **00.6** (agent) Tooling adopt/defer/reject — DECIDED: **adopt** `gofmt` (mandatory gate) + `go vet` + `go test -race` (CI where a C toolchain exists) + Go fuzzing (for `internal/canonjson`/parsing in 02); **defer** `golangci-lint` (add as a CI convenience once packages exist; not a blocking local gate) and `goreleaser` (subject 07 cross-build/release); skills: `plan-from-template` (in use), `captain-hindsight` (per-subject close), `cleanup-audit` (§7 gate), `verify` (behaviour checks) all **adopt**. No MCP server adopted. Baked into §2.
- [x] **00.7** (agent) Dependencies — CURRENT STATE: **zero third-party modules** (stdlib only); `go.mod` has no `require`. Rule for the first dep (`golang.org/x/sys`, arriving with the OS-primitive packages in subject 01): pin the exact version in `go.mod`/`go.sum`, add nothing else without a §4 decision, keep `go mod tidy` clean, and never commit credentials. No approval-gated tooling was installed, so no `manual-actions.md` entry is needed.
- [x] **00.8** (agent) **Create** the Go-era `docs/decisions.md` + `docs/architecture.md` (they don't exist on `main`; the refactor-branch versions are input via `git show`, not a base to edit) capturing **D001–D016** + the transport/state/git-transaction contract + the **legacy inspect/export-only** behavior (D011). Create a fresh `CHANGELOG.md` and `tasks/lessons.md` for the Go era. Bake findings into §2/§6/§7, subject boxes, and lessons. Inventory doc surfaces (README rewrite, architecture doc, skill docs). End with an implementation-readiness summary.
- [x] **00.9** (product-owner) Base decision (D009) — RESOLVED 2026-07-16: fresh branch `interactive-pairing` from `main`; Go greenfield (D013). Mirrored in `manual-actions.md`.

## Hindsight checkpoint
- [x] Captain Hindsight review recorded
- [x] Verdict is `CLOSE`

### Captain Hindsight — subject 00 (2026-07-16)
**Keep:** the greenfield Go skeleton (module `github.com/David-c0degeek/claudex`, Go 1.25.0, `cmd/claudex` + `internal/buildinfo` + `internal/redact`) with a green build/vet/gofmt/test baseline; the evidence-harvest-with-dispositions approach (`docs/evidence-harvest.md`) that keeps 01–06 from rebuilding the retired engine; the decision log (D001–D016) and architecture doc; the leaf `internal/redact` built ahead of any persistence boundary; `.gitattributes` LF policy.

**Fix before closing (done):** CX checkpoint raised 3 rounds of findings, all folded — skeleton hardening (context/write-errors/arity/dirty marker), broadened + tested redaction (OpenAI `sk-proj-`/GitHub/Slack/AWS), the run input/policy contract (D016 + `internal/config`), harvest dispositions + attach schema deltas, the run-input owner, the OS-primitive decisions (flock; atomic-rename guarantee narrowed to process-crash atomicity), the single-envelope human-decision authority, and the 07.10 provider-research relocation. Created `CHANGELOG.md` + permanent `tasks/lessons.md`.

**Record:** D013–D016 added this subject. Push protection blocked a fake token literal → test secrets are now assembled from fragments (a durable lesson, in `tasks/lessons.md`). Provider interactive/telemetry research is deferred to 07.10; usage caps are conditional on managed-launch telemetry (D007).

**Risk:** the harvest names coverage GAPS (submodules, CRLF/gitattributes filters, case-only paths, staged-index dirt, racing edits) that the Go suite must add when subjects 01/04 land — tracked in `docs/evidence-harvest.md` §5. Windows power-loss durability is explicitly out of the current atomic-rename guarantee (revisit in 01 if needed).

**Verdict:** CLOSE (CX checkpoint AGREE on `09f9c5e`, zero blocking/major).

## Progress log
> One line per slice. Date · slice number · box IDs touched · what shipped · how verified · checkpoint commit/push status.

- 2026-07-16 · slice 1 · 00.3 (partial), 00.9 · Baseline on fresh `interactive-pairing` branch (from `main`, D009 resolved). `python -m compileall claudex` OK (Python 3.14.6); `unittest discover -s tests` finds 0 tests — main has no suite. Discovered main is the lean v2 baseline; the 12-module reliability engine + 10 test modules + 6 fixtures live only on the refactor branch (see lessons). Superseded by the Go pivot (D013/D014): the Python baseline is now reference-only; 00.3 is re-scoped to a Go skeleton. Plan committed (dfed322); not yet pushed.
- 2026-07-16 · slice 2 · 00.1, 00.2, 00.3, 00.4, 00.8 · Go skeleton: `go.mod` (module `github.com/David-c0degeek/claudex`, go 1.25.0), `cmd/claudex` (version/help + tests), `internal/buildinfo`. Green baseline verified: `go build ./...`, `go vet ./...`, `gofmt -l .` clean, `go test ./...` pass, `claudex version` runs. Evidence harvested (subagent, via `git show reliability-observability-refactor:*`) → `docs/evidence-harvest.md` (schema contracts, convergence rules, quoted edge vectors for tree-identity/cancellation/redaction/usage/canonical-JSON + coverage GAPS to add). Created Go-era `docs/decisions.md` (D001–D015 + 00.4 OS-primitive/canonical-JSON research notes) + `docs/architecture.md`. Removed Python (`claudex/` 14 files + `pyproject.toml`, kept in git history); README rewritten to honest Go surface. Post-deletion build still green. Ready for CX checkpoint review before ticking boxes. Remaining 00 boxes: 00.5 (provider interactive/telemetry research → scoped to subject 07 readiness), 00.6 (tooling: golangci-lint/goreleaser adopt-decision), 00.7 (dep pin — none yet; `x/sys` lands in 01).
- 2026-07-16 · slice 3 · CX checkpoint REVISE fixes · Addressed all 6 findings: tightened the CLI/buildinfo skeleton (context.Context, write-error→exit 1, arity rejection, dirty-commit marker, buildinfo tests); added `internal/redact` leaf + tests (harvested secret vectors); locked 00.4 OS decisions (flock over fcntl; atomic-rename guarantee narrowed to process-crash atomicity with Windows write-through as an explicit hardening option); added dispositions (ADOPT/ADAPT/CONDITIONAL/RETIRE) + attach-protocol schema deltas to the harvest doc; added D016 (run input + policy contract) with `internal/config` + boxes 01.9/01.10; closed 00.5 (relocated to 07), 00.6 (tooling decided), 00.7 (zero deps + pin rule); created `CHANGELOG.md` + permanent `tasks/lessons.md`; fixed the 10-modules+6-fixtures count. Go build/vet/gofmt/test green. Awaiting CX re-review before ticking 00 boxes + Captain Hindsight close.
- 2026-07-16 · slice 4 · 00.6/00.7 close, redact hardening, 07.10 relocation · Second CX REVISE folded (broadened redaction to OpenAI `sk-proj-`/GitHub/Slack/AWS with table tests; single-envelope human-decision authority; buildinfo `format` dirty test; D016 ordering; `internal/config` boxes). Push protection blocked a fake Slack literal → test secrets assembled from fragments; commit amended (`09f9c5e`). CX checkpoint **AGREE**. Two doc nits fixed (07.10 refs; CHECKPOINT_REVIEW delta envelope-only). All 00 boxes ticked, Captain Hindsight CLOSE recorded, subject 00 marked DONE. Committing close + push, then subject 01.
